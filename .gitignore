# build artifacts
build/
libUniBarCode.so
libUniBarCode.so.*
libUniBarCode.dylib
libUniBarCode.a
*.o
*.obj
*.exe
*.dll
*.so
*.so.*
*.dylib
*.a
*.lib

# coverage
coverage/
lcov.info

# nim cache
nimcache/
rnimcache/
dnimcache/

# docs output
pages/
docs/
*.html
nimdoc.out.css

# python binding artifacts (sources tracked; generated + bundled lib not)
py/unibarcode/_core.c
py/unibarcode/*.so
py/unibarcode/libUniBarCode.so
py/dist/
py/build/
py/*.egg-info/
py/**/__pycache__/
py/.pytest_cache/

# c test/example binaries
tests/c/test_unibarcode
tests/c/test_unitemplate
examples/c/demo

# wasm build artifacts (generated by `nimble wasm`; the hand-written
# dist/unibarcode-api.js wrapper IS tracked).
dist/unibarcode.js
dist/unibarcode.wasm
build/nimcache_wasm/

# os / editor
.DS_Store
*~
.*.swp
*.dSYM

*.log
*.core

# nimib saves a page beside a compiled chapter; not a deliverable.
book/*.html

# nimibook build output. `docs` copies it to pages/, which is what ships.
book/__site/

# What the C demo writes when it runs. Tracked before, so every change to the
# renderer showed up as a binary diff nobody reviews; no other repository in
# the family tracks its example output.
examples/c/demo.png
examples/c/demo.svg
